---
# Mandatory fields.
title: Azure Digital Twins query language reference - Functions
titleSuffix: Azure Digital Twins
description: Reference documentation for the Azure Digital Twins query language functions
author: baanders
ms.author: baanders # Microsoft employees only
ms.date: 03/22/2021
ms.topic: article
ms.service: digital-twins

# Optional fields. Don't forget to remove # if you need a field.
# ms.custom: can-be-multiple-comma-separated
# ms.reviewer: MSFT-alias-of-reviewer
# manager: MSFT-alias-of-manager-or-PM-counterpart
---

# Azure Digital Twins query language reference: Functions

This document contains reference information on **functions** for the [Azure Digital Twins query language](concepts-query-language.md).

## ENDSWITH

A string function. Returns a Boolean indicating whether the first string expression ends with the second.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## IS_DEFINED

A type checking and casting function. Returns a Boolean value indicating if the property has been assigned a value. 

This is supported only when the value is a primitive type. Primitive types include string, Boolean, numeric, or `null`. `DateTime`, object types, and arrays are not supported.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## IS_OF_MODEL

A type checking and casting function. Returns a Boolean value indicating if the specified twin matches the specified model type.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## IS_BOOL

A type checking and casting function. Returns a Boolean value indicating if the type of the specified expression is a Boolean.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## IS_NUMBER

A type checking and casting function. Returns a Boolean value indicating if the type of the specified expression is a number.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## IS_STRING

A type checking and casting function. Returns a Boolean value indicating if the type of the specified expression is a string.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## IS_NULL

A type checking and casting function. Returns a Boolean value indicating if the type of the specified expression is null.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## IS_PRIMITIVE

A type checking and casting function. Returns a Boolean value indicating if the type of the specified expression is a primitive (string, Boolean, numeric, or `null`).

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## IS_OBJECT

A type checking and casting function. Returns a Boolean value indicating if the type of the specified expression is a JSON object.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```

## STARTSWITH

A string function. Returns a Boolean indicating whether the first string expression starts with the second.

### Syntax

### Arguments

### Returns

### Example

The following example...

```sql
<example>
```